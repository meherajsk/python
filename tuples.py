#un changeable

mytuple = ("apple", "banana", "cherry")
print(mytuple)

print(len(mytuple))

#one item tuple remember the comma
thistuple = ("apple",)
print(type(thistuple))

#it can be any data type and contain different data type
#tuple are defined as object with data type tuple


#making tuple using tuple() contructor
thistuple1 = tuple(("apple", "mango", "banana", "orange"))
print(thistuple1)

